# CLAUDE.md

راهنمای Claude Code برای کار در این پروژه.

## پروژه

داشبورد KPI و آنالیتیکس کمپین پیش‌بینی **جام جهانی ۲۰۲۶** اکسیراز («**لیگ ۲۴ عیار**»).
پروژه‌ی **مستقل** است (جدا از سایت برند شخصی فرید/PBRANDING).

- **استک:** HTML/CSS/JS خالص — بدون build، بدون dependency نصبی. `index.html` را مستقیم در مرورگر باز کن.
- **نمودار:** Chart.js از CDN. **فونت:** Vazirmatn از CDN. **زبان:** فارسی، **RTL** تک‌زبانه.
- **KPIهای کمپین:** SignUp + Engagement.

## قانون اصلی

- **به‌روزرسانی داده فقط در `js/data.js`** (آبجکت `CAMPAIGN_DATA`). تمام محاسبات
  (CPA، نرخ تبدیل، درصد، مجموع) در `js/dashboard.js` خودکار است — آن را برای تغییر عدد دست نزن.
- اعداد را **لاتین** بنویس (`14523`)؛ نمایش فارسی خودکار انجام می‌شود. مبالغ به **تومان**.
- متن دیداری را hard-code نکن — از داده‌ی `data.js` بیاید.
- `data.js` در انتها باید `window.CAMPAIGN_DATA = CAMPAIGN_DATA;` داشته باشد
  (چون `const` سطح‌بالا به `window` وصل نمی‌شود و `dashboard.js` آن را از `window` می‌خواند).

## ساختار

```
index.html · README.md · css/dashboard.css · js/data.js · js/dashboard.js
```

## برند (CSS variables در css/dashboard.css)

تم **طلای ۲۴ عیار روی پس‌زمینه‌ی تیره** + لهجه‌های رنگی فیفا ۲۰۲۶.
`--gold: #e8c156` · `--gold-grad` برای المان‌های طلایی · رنگ‌های داده در `PALETTE` داخل `dashboard.js`.
RTL با CSS logical properties؛ به `prefers-reduced-motion` احترام گذاشته شده.

## وضعیت / یادداشت باز

- **ثبت‌نام = ۱۴٬۵۲۳** برای بازه‌ی ۱۴۰۵/۰۳/۱۹–۰۴/۰۱. ⚠️ هنوز مشخص نیست فقط «ورودی کمپین
  جام جهانی» است یا کل ثبت‌نام‌ها — بعد از روشن‌شدن منبع ممکن است جایگاهش در داشبورد عوض شود.
- بقیه‌ی متریک‌ها هنوز صفرند و منتظر داده‌ی دستی کاربر هستند.

## Verify

1. `index.html` را در مرورگر باز کن.
2. RTL، فونت Vazirmatn و رندر هر سه نمودار (خطی + دو دونات) و جدول کانال‌ها را چک کن.
3. یک عدد در `js/data.js` عوض کن و رفرش → اطمینان از به‌روزرسانی کارت‌ها و مقادیر محاسبه‌شده.
4. ریسپانسیو: ۳۷۵px (تک‌ستون) و ۱۴۴۰px (سه‌ستونه).
